rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Locked down — server-side access only via Admin SDK
      allow read, write: if false;
    }
  }
}
